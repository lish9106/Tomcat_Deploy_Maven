pipeline {
agent { label 'J-slave-1' } 
stages {
    stage('Clone') {
        steps {
            echo 'Cloning the code..'
            git 'https://github.com/lish9106/Tomcat_Deploy_Maven.git'
        }
    }
}
}
