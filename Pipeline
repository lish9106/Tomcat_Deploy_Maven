pipeline {
agent { label 'J-slave-1' } 
stages {
    stage('Clone') {
        steps {
            echo 'Cloning the code..'
            git 'https://github.com/PareshSwain/DeploytoTomcatPlugin.git'
        }
    }
}
